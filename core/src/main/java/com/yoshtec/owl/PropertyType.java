package com.yoshtec.owl;

/** Enum for Property Types. */
public enum PropertyType {
  /** OWL object properties. */
  OBJECT,
  /** OWL data properties. */
  DATA,
  /** Special Property Type for handling the individual name. */
  ID
}
